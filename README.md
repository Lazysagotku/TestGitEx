кнопка появилась (вернулась).
при переходе в задачу лишние строки не добавляются. но и строка, из которой осуществлён переход, не уходит из таблицы.
прокрутка появилась. ширина столбцов ужалась. 
при первом запуске приложение по прежнему в центре экрана.
уведомления о новых задачах появляется один раз, при первом запуске приложения и больше не появляется.
при разворачивании окна (после даблклика на строчку "Сумма по времени") доп. таблица уходит за границы окна.
