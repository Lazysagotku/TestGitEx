1. перезагрузка оставляют иконку в панели 
2. Таблица под 4 и 5 строкой - посмотреть под отладкой желательно , но так же сравнить все размерности
3. "Сделать прочитанным" опять делает все прочитанным, а не по-одному
4. После перезапуска локацию окна сохранить и там же воспроизвести
5.private void LoadIdTasksData(string date)
        {
            _idTasksCache = IdTasksUserTable.GetData(date);
        }
GetData(date) 
Ошибка	CS1061	"IdTasksUserTable" не содержит определения "GetData", и не удалось найти доступный метод расширения "GetData", принимающий тип "IdTasksUserTable" в качестве первого аргумента (возможно, пропущена директива using или ссылка на сборку)


6. private void RenderIdTasksTable()
        {
            IdTasksUserTable.Render(_idTasksCache);
        }
IdTasksUserTable.Render(_idTasksCache);
Ошибка	CS1061	"IdTasksUserTable" не содержит определения "Render", и не удалось найти доступный метод расширения "Render", принимающий тип "IdTasksUserTable" в качестве первого аргумента (возможно, пропущена директива using или ссылка на сборку)


7. И куда лучше вставить новые  эти асинки? В прошлый раз  я вставил написанные нами асинки и прогрузка то осталось такой же - пока отрисутеся и пока данные запросятся и проставятся черный экран будет, а вот когда закрывать, то таблица сразу пропала, но так ка вторая таблица не пропадала и не была прописана корректная логика,
то форма закрывалась со второго или третьего раза.
